import{_ as o,c as i,o as t,ag as d}from"./chunks/framework.CA53vVaN.js";const h=JSON.parse('{"title":"Deploying the docs (GitHub Pages / Netlify)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/deploy.md","filePath":"guide/deploy.md"}'),s={name:"guide/deploy.md"};function l(c,e,a,p,n,r){return t(),i("div",null,[...e[0]||(e[0]=[d('<h1 id="deploying-the-docs-github-pages-netlify" tabindex="-1">Deploying the docs (GitHub Pages / Netlify) <a class="header-anchor" href="#deploying-the-docs-github-pages-netlify" aria-label="Permalink to &quot;Deploying the docs (GitHub Pages / Netlify)&quot;">​</a></h1><p>We provide a CI sample for GitHub Pages in <code>.github/workflows/deploy-docs.yml</code>. It builds the VitePress site and publishes <code>docs/.vitepress/dist</code> to <code>gh-pages</code>.</p><p>GitHub Pages notes:</p><ul><li>To host at <code>https://USERNAME.github.io/REPO/</code>, set <code>base: &quot;/REPO/&quot;</code> in <code>docs/.vitepress/config.ts</code>.</li><li>The provided GitHub Action uses <code>peaceiris/actions-gh-pages</code> to publish. Set up a branch if your repo uses different publishing rules.</li></ul><p>Netlify / Vercel:</p><ul><li>Set the build command to: <code>pnpm docs:build</code></li><li>Set the publish directory to: <code>docs/.vitepress/dist</code></li></ul><p>Netlify specifics:</p><ul><li>Engine: Node 18+ (Netlify uses <code>NODE_VERSION</code> from <code>netlify.toml</code> or set <code>NODE_VERSION</code> in site settings)</li><li>Use <code>VITEPRESS_BASE=/</code> if your Netlify site is hosted at a root domain; otherwise add <code>VITEPRESS_BASE=/repo-name/</code> for sub-path deployments (e.g., GitHub Pages). You can set this as a Netlify environment variable in Site settings -&gt; Build &amp; deploy -&gt; Environment.</li><li>To keep the <code>playground/</code> site available for preview builds, configure a <code>[context.deploy-preview]</code> in <code>netlify.toml</code> with <code>command: pnpm run play:build</code> and <code>publish: playground/dist</code>.</li></ul><p>Tricks:</p><ul><li>Use <code>docs:build</code> in your CI to catch build-time issues early</li><li>Keep dev-only assets out of <code>docs/.vitepress/public</code> if they are large; instead store them in <code>public/</code> and reference by absolute path</li></ul>',10)])])}const g=o(s,[["render",l]]);export{h as __pageData,g as default};
