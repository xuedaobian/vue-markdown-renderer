import{_ as o,c as t,o as i,ag as s}from"./chunks/framework.CA53vVaN.js";const h=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"guide/troubleshooting.md","filePath":"guide/troubleshooting.md"}'),n={name:"guide/troubleshooting.md"};function r(a,e,d,l,c,p){return i(),t("div",null,[...e[0]||(e[0]=[s(`<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h1><p>If something breaks, here are common fixes:</p><ul><li>If you get <code>window is not defined</code> during SSR, wrap components in <code>&lt;client-only&gt;</code> for Nuxt and use the <code>onMounted</code> guard for Vite SSR.</li><li>Install <code>katex</code> when math rendering fails, and import <code>katex/dist/katex.min.css</code> in your app entry.</li><li>For Mermaid issues, upgrade or pin <code>mermaid</code> to a version &gt;= 11 and check asynchronous render logs.</li><li>For performance concerns, check that <code>viewportPriority</code> is true and avoid too many heavy nodes in a single mount.</li></ul><p>If unsure, reproduce issue using the playground (or the hosted quick test) and then open an issue with stack trace and minimal markdown sample.</p><p>Hosted quick test: <a href="https://vue-markdown-renderer.netlify.app/test" target="_blank" rel="noreferrer">https://vue-markdown-renderer.netlify.app/test</a></p><p>Open a new issue (quick link): <a href="https://github.com/Simon-He95/vue-markdown-renderer/issues/new?template=bug_report.yml" target="_blank" rel="noreferrer">https://github.com/Simon-He95/vue-markdown-renderer/issues/new?template=bug_report.yml</a></p><h2 id="common-problems-faq" tabindex="-1">Common problems (FAQ) <a class="header-anchor" href="#common-problems-faq" aria-label="Permalink to &quot;Common problems (FAQ)&quot;">​</a></h2><ul><li><p>Tailwind / CSS utilities overriding styling: If your project uses Tailwind or a component library (e.g., shadcn), utility classes or global styles may override the library&#39;s styles. See the Tailwind integration guide for recommended import ordering and strategies: <code>/guide/tailwind</code>.</p><p>Quick fixes:</p><ul><li>Import <code>vue-renderer-markdown/index.css</code> inside a <code>@layer components { ... }</code> block (see the Tailwind page). This controls style ordering.</li><li>Consider setting a <code>prefix</code> in Tailwind config (e.g., <code>tw-</code>) to avoid collisions with component library class names.</li><li>Use scoped selectors or <code>:deep</code> to target only the elements you need to override.</li></ul></li><li><p>Want to tweak looks? You can customize via CSS variables in <code>src/index.css</code> (e.g., <code>--vscode-editor-background</code>, <code>--vscode-editor-foreground</code>) or override component classes in your app&#39;s stylesheet. Use the <code>@apply</code> helper or your own stylesheet to keep style rules isolated.</p></li><li><p>Slots first: if the styling you want needs layout changes inside a component, check whether the component provides a named slot (e.g., <code>header-left</code>, <code>header-right</code>, <code>loading</code>). Slots are an easy and stable escape hatch that let you re-render interior markup without overriding internals.</p></li><li><p>Still not enough? Use <code>setCustomComponents(id, mapping)</code> to replace an entire renderer for a node with your own Vue component. See the <code>Advanced</code> guide for <code>setCustomComponents</code> usage and how to scope them to a <code>custom-id</code>. Quick example (replace the renderer for <code>code_block</code> nodes scoped to <code>my-docs</code>):</p></li></ul><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { setCustomComponents } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vue-renderer-markdown&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">setCustomComponents</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;my-docs&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  code_block: MyCustomCodeBlock,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>This will cause all <code>MarkdownRender</code> instances with <code>custom-id=&quot;my-docs&quot;</code> to render the <code>code_block</code> node using <code>MyCustomCodeBlock</code> instead of the default. See <code>Advanced</code> for more examples.</p><ul><li><p>Repro / report flow: When you run into rendering anomalies or errors, try to reproduce them with the <code>playground</code> app and a minimal markdown sample. If it&#39;s still broken, run unit tests locally with <code>pnpm test</code> to rule out regressions. Then open an issue with:</p><ol><li>Minimal markdown example that reproduces the problem (include in the issue body or link to a gist).</li><li>Steps to reproduce (playground link or <code>playground</code> instructions), and the environment (browser, Node version, Vite/Nuxt).</li><li>Any error stack traces and relevant console output.</li></ol><p>Prefer to share a small <code>playground</code> reproduction and include a link; maintainers will triage and ask for more context if needed.</p><p>Extra tip: if you can write a unit/integration test that reproduces the bug, add it under the <code>test/</code> folder and run <code>pnpm test</code> locally — this is the fastest way for maintainers to validate and fix regressions.</p></li></ul>`,11)])])}const m=o(n,[["render",r]]);export{h as __pageData,m as default};
